services:
  - type: web
    name: bmpmode-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: CORS_ORIGINS
        sync: false
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 8000
